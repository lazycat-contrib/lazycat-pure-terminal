package: community.lazycat.webshell.pure-terminal
version: 0.2.3
name: Pure Terminal
description: LightOS WebShell provider backed by Rust, WebSocket terminal I/O, and Connect control APIs
min_os_version: v1.5.2
hidden_from_launcher: true
permissions:
  required:
    - lightos.manage
locales:
  zh:
    name: Pure Terminal
    description: 基于 Rust、WebSocket 终端数据面和 Connect 控制面的 LightOS WebShell provider
